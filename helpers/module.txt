npm init
npm i bootstrap-icons



check id_type if exist : if (id_type != 1 && id_type !=2 && id_type != 3){ $error = "choisissez parmis les types d'évènements existants"} 


delete folder album 
check responsive 
add button participant event for nadine 
check if add album type == 2/3
check if name picture exist
change all "picture" to "image"




if admin && no album 
$add_album = ajoutez des photos pour que l'évènements s'affiche dans la gallerie , sinon supprimer l'évènement 
return false
if album 
return true


Allons cueillir des champignons !!